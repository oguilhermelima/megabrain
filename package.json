{
  "name": "megabrain",
  "private": true,
  "version": "0.2.4",
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/cli/index.ts --outfile .build/megabrain",
    "build:node": "bun build --target=node src/cli/index.ts --outfile .build/megabrain.mjs",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "devDependencies": {
    "@types/bun": "1.4.2",
    "@types/ws": "^8.18.1",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
