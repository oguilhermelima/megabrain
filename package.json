{
  "name": "megabrain",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/cli/index.ts --outfile .build/megabrain",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "1.4.2",
    "typescript": "7.0.2"
  }
}
